server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    #当前的eurera服务是单击版的
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/